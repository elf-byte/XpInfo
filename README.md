How to use

Put the two files in Interface/AddOns/XPInfo/
/reload or restart the client
The little frame appears in the middle of the screen (you can drag it)
/xpinfo toggles it on/off

It uses the exact same session math that pfUI uses (start XP + start time on login / level-up adjustment).
